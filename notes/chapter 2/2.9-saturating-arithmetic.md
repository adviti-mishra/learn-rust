# Saturating arithmetic 

## 1. wrapping_ methods
per operation basis: 
```
let sum = x.wrapping_add(y);
```

## 2. saturating_ methods
rounds up or down to MAX or MIN value of variable 
```
let sum = x.saturating_add(y);
assert_eq!(sum, 255);
```